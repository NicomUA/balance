import { ExchangeInterceptor } from './exchange.interceptor';

describe('ExchangeInterceptor', () => {
  it('should be defined', () => {
    // expect(new ExchangeInterceptor()).toBeDefined();
  });
});
